---
// about.astro - 关于页（第二作品面：叙事 + 立场，仍服务阅读）
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icon.astro';

const basePath = import.meta.env.BASE_URL.replace(/\/$/, '');
const writingUrl = `${basePath}/#blog`;

const principles = [
  {
    icon: 'book-open' as const,
    title: '先写字',
    body: '站点是放文字的地方。工程实践、工具踩坑、读书和日常碎片都可以，不追热点，也不必每篇都有完整论证。',
  },
  {
    icon: 'layers' as const,
    title: '尽量静态',
    body: 'Markdown 进，Astro 出，GitHub Pages 托管。没有后台、没有数据库、没有必须每天盯的仪表盘。',
  },
  {
    icon: 'shield-check' as const,
    title: '检查要诚实',
    body: '链接、草稿、canonical、RSS 要有人真查。绿灯如果只会点头，就不如没有。发布前优先跑 verify。',
  },
  {
    icon: 'feather' as const,
    title: '少依赖',
    body: '图标构建期进包，主题靠 CSS，不为装饰再拉一串 CDN。功能能少就少，门禁坏了再修。',
  },
];
---

<Layout
  title="About - Lucid Hubble"
  description="关于 Lucid：服务端工程、开发工具、读书与日常随笔。这个静态站怎么长成现在这样。"
>
  <article class="about-container">
    <header class="about-header">
      <p class="about-eyebrow mono-badge">About</p>
      <h1>关于 Lucid</h1>
      <div class="about-rule" aria-hidden="true"></div>
      <p class="about-deck">
        写服务端，也折腾工具。这个站用来放不适合塞进信息流里的长一点的记录。
      </p>
    </header>

    <section class="about-content">
      <p>
        我是 <strong>Lucid</strong>。平时主要和后端、基础设施打交道，偶尔会把本地开发环境搓得比较花。写东西没有固定栏目：有时是线上问题，有时是自托管工具，有时只是下班后还没散掉的一点想法。
      </p>
      <p>
        开这个站，不是为了做成产品，也不是为了喂算法。就是想给一些话找个稳定的地址——以后还能打开，搜索引擎也能看见，自己回头翻也方便。
      </p>
      <p>
        页面普通一点没关系。链接别坏、草稿别漏、订阅能用，对我来说就够了。想看文章的话，可以从
        <a href={writingUrl}>Writing</a> 进去。
      </p>
    </section>

    <section class="about-principles" aria-labelledby="about-principles-title">
      <div class="section-heading about-principles-heading">
        <h2 id="about-principles-title" class="section-title">How this site works</h2>
      </div>

      <div class="dimensions">
        {principles.map((item) => (
          <div class="dimension-card">
            <h3>
              <Icon name={item.icon} class="dimension-icon" size={18} strokeWidth={1.75} />
              <span>{item.title}</span>
            </h3>
            <p>{item.body}</p>
          </div>
        ))}
      </div>
    </section>

    <p class="about-footnote">
      栈很短：Astro、Markdown、静态产物。能长期保留、能被正常发现，就继续用。
    </p>
  </article>
</Layout>
